# infra/ — Azure resource provisioning

- `setup.sh`    — Idempotent Azure CLI script to create Storage container, ACR, App Service, Key Vault entries
- `teardown.sh` — Delete the resource group (clean credit reset)
